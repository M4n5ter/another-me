package en

const ScreenshotDescription = "Capture a screenshot of the default desktop, format like: data:image/png;base64,..."

const MoveMouseDescription = "Move the mouse to the specified coordinates."

const MoveMouseArgXDescription = "x coordinate"

const MoveMouseArgYDescription = "y coordinate"

const MouseLocationDescription = "Get the current mouse location."

const DragDescription = "Drag the mouse to the specified coordinates."

const DragArgXDescription = "x coordinate"

const DragArgYDescription = "y coordinate"

const ScrollDescription = "Scroll the mouse to the specified coordinates."

const ScrollArgToyDescription = "y coordinate"

const ScrollArgNumDescription = "scroll times"

const ScrollArgMsSleepDescription = "scroll interval(milliseconds)"

const ScrollArgToxDescription = "x coordinate"

const ScrollRelativeDescription = "Scroll the mouse to the specified coordinates relative to the current position."

const ScrollRelativeArgXDescription = "x coordinate"

const ScrollRelativeArgYDescription = "y coordinate"

const ScrollRelativeArgMsDeplayDescription = "scroll interval(milliseconds)"

const ScrollDirectionDescription = "Scroll the mouse to the specified direction."

const ScrollDirectionArgXDescription = "x coordinate"

const ScrollDirectionArgDirectionDescription = "scroll direction"

const ClickDescription = "Click the mouse."

const ClickArgButtonDescription = "mouse button"

const ClickArgDoubleDescription = "is double click"

const ToggleMouseButtonDescription = "Toggle the mouse button."

const ToggleMouseButtonArgButtonDescription = "mouse button"

const ToggleMouseButtonArgUpDescription = "is up"

const ToggleKeyDescription = "Toggle the key."

const ToggleKeyArgUpDescription = "is up"

const ToggleKeyArgKeysDescription = "keys"

const KeyTapDescription = "Tap the key."

const KeyTapArgKeysDescription = "keys"

const KeySleepMilliDescription = "Key sleep(milliseconds)."

const KeySleepMilliArgMsDescription = "sleep time(milliseconds)"